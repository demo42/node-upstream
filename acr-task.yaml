version: v1.0.0
steps:
  - build: >
      -f ./Dockerfile
      -t {{.Run.Registry}}/upstream/node:{{.Run.ID}}
      -t {{.Run.Registry}}/upstream/node:9-alpine
      .
  - push: 
    - "{{.Run.Registry}}/upstream/node:{{.Run.ID}}"
    - "{{.Run.Registry}}/upstream/node:9-alpine"
