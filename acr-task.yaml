version: v1.0.0
steps:
  - build: >
      -f ./Dockerfile
      -t {{.Run.Registry}}/library/node:{{.Run.ID}}
      -t {{.Run.Registry}}/library/node:9-alpine
      .
  - push: 
    - "{{.Run.Registry}}/library/node:{{.Run.ID}}"
    - "{{.Run.Registry}}/library/node:9-alpine"
